# Roadmap

## Next up
- Virtualized scrolling beyond current render cap for very large libraries.
- Queue reordering via drag-and-drop.
- Better album art heuristics (folder-level fallback scanning and cache).
- Optional persisted queue and multi-select state.

## Later
- Keyboard shortcuts for multi-select ranges.
- Pluggable metadata enrichers (tags sidecar / JSON imports).
- Optional service-worker package for fully cached offline app shell.
- Advanced playlist diagnostics export.
